{
  "name": "poe-contract",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "register:sepolia": "hardhat run scripts/register.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.2",
    "dotenv": "^17.2.3",
    "ethers": "^6.15.0",
    "hardhat": "^3.0.9"
  }
}